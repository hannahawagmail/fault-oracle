# SPDX-License-Identifier: Apache-2.0
apiVersion: v1
kind: ServiceAccount
metadata:
  name: hw-fault-exporter
  namespace: monitoring
  labels:
    app.kubernetes.io/name: hw-fault-exporter
---
# hw-fault-exporter only reads /sys via hostPath volume. No Kubernetes API
# access is needed, so no ClusterRole/ClusterRoleBinding is required.
